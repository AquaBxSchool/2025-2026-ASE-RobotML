{
	"name": "robot-ml-workspace",
	"description": "Base workspace package",
	"version": "0.0.1",
	"type": "module",
	"private": true,
	"scripts": {
		"clean": "yarn workspaces run clean",
		"build": "yarn fmt && yarn workspaces run build && yarn test",
		"test": "yarn workspaces run test",
		"mutation": "yarn workspaces run mutation",
		"cli": "robot-ml-cli",
		"fmt": "biome format --write"
	},
	"devDependencies": {
		"@types/node": "^25.1.0",
		"@biomejs/biome": "^2.3.13",
		"rolldown": "^1.0.0-rc.2",
		"rolldown-plugin-dts": "^0.21.7",
		"shx": "^0.4.0",
		"vitest": "^4.0.18"
	},
	"workspaces": [
		"packages/language",
		"packages/extension",
		"packages/cli"
	]
}
