void main() {
    let d = 10
    SetSpeed(d centimeter/second)
    Move(Forward d centimeter)
    Move(Left d centimeter)
    Move(Right d centimeter)
    Move(Backward d centimeter)
    Move(LowerLeft d centimeter)
    Move(UpperRight d centimeter)
    Move(UpperLeft d centimeter)
    Move(LowerRight d centimeter)
    Rotate(-d degrees)
    let _ = GetDistance(centimeter)
    let s: float = GetSpeed(millimeter/second)
    SetSpeed(s meter/millisecond)
}
