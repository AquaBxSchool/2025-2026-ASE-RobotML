void main() {

    SetSpeed(30, millimeter / second)

    let i = 0

    if (GetDistance(centimeter) > 10) {
        Move(Forward, 10, centimeter)
    }
    else {
        Rotate(90, degrees)
    }

    // while (i < 4) {
    //     Move(Forward, 300)
    //     i = i +1
    // }

}